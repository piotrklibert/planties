use Mix.Config

# You can configure for your application as:
#     config :planties, key: :value
# And access this configuration in your application as:
#     Application.get_env(:planties, :key)

# config :logger,
#   backends: [{LoggerFileBackend, :log}]

# config :logger, :log,
#   path: "error.log",
#   level: :debug

config :sasl, errlog_type: :error

# import_config "#{Mix.env}.exs"
