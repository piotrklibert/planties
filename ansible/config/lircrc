begin
    prog = myprog
    button = KEY_1
    config = 1
end
begin
    prog = myprog
    button = KEY_2
    config = 2
end
begin
    prog = myprog
    button = KEY_3
    config = 3
end
begin
    prog = myprog
    button = KEY_4
    config = 4
end
begin
    prog = myprog
    button = KEY_5
    config = 5
end
begin
    prog = myprog
    button = KEY_6
    config = 6
end
begin
    prog = myprog
    button = KEY_7
    config = 7
end
begin
    prog = myprog
    button = KEY_8
    config = 8
end
begin
    prog = myprog
    button = KEY_9
    config = 9
end
begin
    prog = myprog
    button = KEY_0
    config = 0
end
begin
    prog = myprog
    button = KEY_NEXT
    config = next
end
begin
    prog = myprog
    button = KEY_PREVIOUS
    config = prev
end
begin
    prog = myprog
    button = KEY_FASTFORWARD
    config = forward
end

begin
    prog = myprog
    button = KEY_REWIND
    config = backward
end
begin
    prog = myprog
    button = KEY_PLAYPAUSE
    config = play
end
begin
    prog = myprog
    button = KEY_VOLUMEUP
    config = vol+
end
begin
    prog = myprog
    button = KEY_VOLUMEDOWN
    config = vol-
end
